# Netlify 部署检查清单

使用此清单确保部署过程无误。按顺序完成每一项。

---

## 前置检查

- [ ] 确保代码已提交到 GitHub
  ```bash
  git status          # 应该显示 "nothing to commit"
  git push            # 上传最新代码
  ```

- [ ] 验证项目结构
  ```
  GitHub 仓库根目录/
  └── design-case-collection/      ← 这个文件夹很重要
      ├── src/
      ├── package.json
      ├── vite.config.ts
      └── netlify.toml              ← 应该已经存在
  ```

- [ ] 确认 netlify.toml 存在
  ```bash
  cat netlify.toml                 # 应该显示配置内容
  ```

- [ ] 本地构建成功
  ```bash
  npm run build                    # 不报错
  ls dist/                         # 应该有 index.html
  ```

---

## Netlify 部署步骤

### 第 1 步：访问 Netlify

- [ ] 打开 https://netlify.com
- [ ] 点击 "Log in"
- [ ] 选择 "GitHub"
- [ ] 授权 Netlify 访问你的 GitHub

**预期**：登录后进入 Netlify 仪表盘

---

### 第 2 步：创建新项目

- [ ] 点击 "Add new site"
- [ ] 选择 "Import an existing project"
- [ ] 选择 "GitHub"
- [ ] 授权 Netlify 访问你的 GitHub 仓库

**预期**：看到你的 GitHub 仓库列表

---

### 第 3 步：选择仓库

- [ ] 搜索 "design-case-collection"
- [ ] 点击仓库名称选择它
- [ ] 确认所有者是你的 GitHub 账号

**预期**：看到仓库的分支列表

---

### 第 4 步：配置构建设置（最重要！）

#### 基本设置

- [ ] 确认 Branch: `main` 或 `master`（默认正确）

#### 构建设置

- [ ] **Base directory**: `design-case-collection`
  - 不要填空
  - 不要填 `./design-case-collection`
  - 就是 `design-case-collection`

- [ ] **Build command**: `npm run build`
  - 不要填 `npm install && npm run build`
  - 不要填 `npm build`
  - 就是 `npm run build`

- [ ] **Publish directory**: `dist`
  - 不要填 `design-case-collection/dist`
  - 不要填 `./dist`
  - 就是 `dist`

**预期**：看到三个字段都正确填写

```
Base directory (optional):           design-case-collection
Build command:                       npm run build
Publish directory:                   dist
```

---

### 第 5 步：可选的环境变量

- [ ] 不需要添加环境变量（跳过此步）

**预期**：看到已配置的字段

---

### 第 6 步：部署

- [ ] 检查所有设置无误
- [ ] 点击 **"Deploy site"** 按钮
- [ ] 等待部署完成

**预期**：看到构建日志滚动，最后显示 "Site published"

```
⏳ Building...
✓ Dependencies installed
✓ Building site...
✓ Site published
```

---

## 部署完成后

### 第 7 步：获取部署链接

- [ ] 看到成功提示信息
- [ ] 复制部署链接
  ```
  Your site is live at: https://xxxxx.netlify.app
  ```

**预期**：获得一个 netlify.app 链接

---

### 第 8 步：验证部署

#### 访问应用

- [ ] 打开部署链接
- [ ] 应该看到应用主页
- [ ] 页面加载正确
- [ ] 样式应该显示（有颜色和布局）

#### 测试路由（关键！）

- [ ] 点击应用中的导航链接
- [ ] 页面应该正确加载
- [ ] **按 F5 刷新页面**
- [ ] 刷新后不应该出现 404 错误
- [ ] 页面内容应该保存

#### 检查浏览器控制台

- [ ] 按 F12 打开开发者工具
- [ ] 切换到 Console 标签
- [ ] 不应该看到红色错误信息

#### 检查网络请求

- [ ] 在 Network 标签中查看
- [ ] 所有资源应该返回 200 状态码
- [ ] 没有 404 错误
- [ ] JavaScript 文件应该加载

**预期**：
✓ 应用正常工作
✓ 路由正常
✓ 样式正确
✓ 无错误

---

## 常见问题排查

### 问题：仓库找不到

- [ ] 检查是否已授权 Netlify 访问你的 GitHub
- [ ] 确认登录的 GitHub 账号是正确的
- [ ] 尝试重新授权

**解决**：点击 GitHub 重新连接

---

### 问题：构建失败

- [ ] 点击部署 → "Deploy log"
- [ ] 搜索红色错误信息
- [ ] 常见原因：
  - [ ] Base directory 错误
  - [ ] Build command 错误
  - [ ] package.json 有语法错误

**解决**：
1. 修复错误
2. 推送到 GitHub
3. 在 Netlify 中点击 "Trigger deploy"

---

### 问题：部署成功但看不到内容

- [ ] 检查 Publish directory 是否设置为 `dist`
- [ ] 硬刷新页面：Ctrl+Shift+R（Windows/Linux）或 Cmd+Shift+R（Mac）
- [ ] 清除浏览器缓存

**解决**：Netlify Dashboard → Site settings → 修改 Publish directory

---

### 问题：路由显示 404

- [ ] 确保 netlify.toml 已提交到 GitHub
- [ ] 检查 netlify.toml 的重定向规则是否正确
- [ ] 点击 "Trigger deploy" 重新部署

**重定向规则应该是**：
```toml
[[redirects]]
from = "/*"
to = "/index.html"
status = 200
```

---

### 问题：样式加载失败

- [ ] 打开浏览器开发者工具（F12）
- [ ] 查看 Network 标签
- [ ] 找到 CSS 文件的 URL
- [ ] 确保返回 200 状态码
- [ ] 检查路径是否正确

**常见原因**：
- Publish directory 设置错误
- CSS 文件未正确输出

---

## 最终验收标准

部署成功的标志（全部打勾）：

- [ ] ✓ Netlify 显示绿色对勾（部署成功）
- [ ] ✓ 获得了 netlify.app 链接
- [ ] ✓ 访问链接能打开应用
- [ ] ✓ 页面样式正确显示
- [ ] ✓ 点击导航链接工作正常
- [ ] ✓ 刷新页面不出现 404
- [ ] ✓ 浏览器控制台无红色错误
- [ ] ✓ 所有功能正常工作

**如果全部打勾，说明部署成功！🎉**

---

## 后续步骤

- [ ] （可选）绑定自定义域名
  ```
  Netlify Dashboard → Site settings → Domain management
  ```

- [ ] （可选）设置 CI/CD 通知
  ```
  Netlify Dashboard → Integrations → Slack（如果使用 Slack）
  ```

- [ ] （可选）启用分支预览
  ```
  Netlify Dashboard → Site settings → Build & deploy
  ```

- [ ] 分享部署链接给用户/客户
  ```
  https://xxxxx.netlify.app
  ```

---

## 如果仍然有问题

**第 1 步**：查看 Netlify 构建日志
```
Netlify Dashboard → Deployments → 点击部署 → Deploy log
```

**第 2 步**：本地验证
```bash
cd design-case-collection
npm install
npm run build
# 应该成功生成 dist 文件夹
```

**第 3 步**：查阅文档
- 参考：NETLIFY_STEP_BY_STEP.md（详细步骤）
- 参考：NETLIFY_QUICK_START.md（快速参考）

**第 4 步**：官方支持
- Netlify 文档：https://docs.netlify.com
- React 部署指南：https://docs.netlify.com/integrations/frameworks/react/

---

## 预期时间表

| 步骤 | 时间 |
|------|------|
| 登录 Netlify | 1-2 分钟 |
| 选择仓库 | 1-2 分钟 |
| 配置构建设置 | 2-3 分钟 |
| 开始部署 | 点击按钮 |
| 首次构建 | 3-5 分钟 |
| **总计** | **10 分钟** |

---

## 重要提醒

✓ **netlify.toml 很重要** - 不要删除或修改
✓ **Base directory 很关键** - 必须是 `design-case-collection`
✓ **刷新页面测试** - 这是最重要的验收标准
✓ **查看构建日志** - 问题诊断的关键

---

**现在开始吧！访问 https://netlify.com 并按此清单操作。**

预计 10 分钟内，你的应用就会在线！💪
