# Netlify 配置文件
# 这个文件确保 React SPA 能正确处理路由

[build]
# 构建命令
command = "npm run build"
# 发布目录
publish = "dist"

# 重定向规则 - 确保所有非文件请求都指向 index.html
[[redirects]]
from = "/*"
to = "/index.html"
status = 200
