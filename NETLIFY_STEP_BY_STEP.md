# Netlify 部署 - 完整步骤指南（带截图提示）

## 目标
在 Netlify 上成功部署 React 应用，替代失败的 Vercel 部署。

## 为什么使用 Netlify？

**Netlify 相比 Vercel 的优势**：
- ✓ 原生支持 React SPA（Single Page Application）
- ✓ 自动处理 URL 重写（无需手动配置）
- ✓ 更简单的配置文件（netlify.toml vs vercel.json）
- ✓ 更少的部署失败案例
- ✓ 免费套餐功能齐全

---

## 步骤 1：准备工作

### 确保代码已提交到 GitHub

```bash
# 在项目目录中检查 Git 状态
git status
# 如果有未提交的文件
git add .
git commit -m "Add Netlify configuration"
git push
```

**已自动创建的配置**：
- ✓ `netlify.toml` - 部署配置
- ✓ 项目构建已验证
- ✓ dist 文件夹已生成

---

## 步骤 2：访问 Netlify

1. 打开浏览器，访问：**https://netlify.com**

2. 选择登录方式：
   - 点击 "Log in"
   - 选择 "GitHub" （推荐，因为项目在 GitHub）
   - 授权 Netlify 访问你的 GitHub 账户

---

## 步骤 3：创建新项目

### 方法 A：从仪表盘创建（推荐）

1. 登录后进入 Netlify 仪表盘
2. 找到 "Add new site" 按钮
3. 点击 "Import an existing project"

```
[仪表盘] → [Add new site] → [Import an existing project]
```

### 方法 B：直接连接 GitHub

1. 点击 "Connect to GitHub"
2. 授权 Netlify 访问你的 GitHub 仓库
3. 选择 `design-case-collection` 仓库

---

## 步骤 4：配置构建设置

这是最关键的步骤！请按照以下设置，**一个都不能错**：

### 基本信息

| 字段 | 值 | 说明 |
|------|-----|------|
| **Repository** | `1784810963-afk/design-case-collection` | GitHub 仓库 |
| **Branch** | `main` 或 `master` | 部署分支 |

### 构建设置

```
┌─────────────────────────────────────────┐
│ Build settings                          │
├─────────────────────────────────────────┤
│                                         │
│ Base directory (optional):              │
│ [design-case-collection___________]    │
│ (项目所在的文件夹名称)                 │
│                                         │
│ Build command:                          │
│ [npm run build________________]         │
│                                         │
│ Publish directory:                      │
│ [dist_____________________]             │
│ (相对于 Base directory 的路径)         │
│                                         │
└─────────────────────────────────────────┘
```

### 详细说明

**Base directory**: `design-case-collection`
- 这告诉 Netlify 到哪个文件夹去执行构建
- GitHub 仓库的根目录包含了 `design-case-collection` 文件夹
- 不要填空或填其他路径

**Build command**: `npm run build`
- 执行 package.json 中的 build 脚本
- 会自动运行 TypeScript 编译和 Vite 构建
- 输出到 `dist` 文件夹

**Publish directory**: `dist`
- 这是构建后的输出目录
- Netlify 会将此目录的内容作为网站发布
- 不要添加前缀，保持 `dist`

---

## 步骤 5：高级设置（可选）

### 环境变量（如果需要）

如果应用需要环境变量：

```
点击 "Show advanced"
↓
点击 "New variable"
添加键值对：
  Key: REACT_APP_API_URL
  Value: https://api.example.com
```

通常不需要此步骤，除非有 API 端点或密钥。

### Environment

可以跳过此步骤。

---

## 步骤 6：部署

1. 检查所有设置无误
2. 点击 **"Deploy site"** 按钮
3. 等待部署完成（通常 3-5 分钟）

部署过程中会看到：
```
⏳ Building...
✓ Dependencies installed
✓ Build command executed
✓ Site published
✓ Deployed to [generated-name].netlify.app
```

---

## 步骤 7：获取部署链接

部署完成后，你会看到：

```
✓ Site published
Your site is live at: https://your-unique-name.netlify.app
```

**这就是你的应用链接！**

---

## 验证部署成功

### 1. 访问主页
- 打开部署链接：`https://your-unique-name.netlify.app`
- 应该看到应用主页加载

### 2. 测试路由（重要！）
- 点击应用中的各个导航链接
- 页面应该正确加载
- **最重要**：刷新页面（按 F5）
- 不应该出现 404 错误

### 3. 检查浏览器控制台
- 按 F12 打开开发者工具
- 检查 Console 标签
- 应该没有红色错误信息
- 检查 Network 标签
- 所有资源应返回 200 状态码

### 4. 检查样式和功能
- CSS 应该正确应用（界面有颜色和布局）
- JavaScript 功能应该正常工作
- 响应式设计应该在手机上也能正常显示

---

## 常见问题与解决方案

### ❌ 问题 1：部署失败 - "Base directory not found"

**原因**：Base directory 路径错误

**解决方案**：
```
检查项目结构：
.
├── design-case-collection/          ← 这个文件夹
│   ├── src/
│   ├── package.json
│   ├── vite.config.ts
│   ├── netlify.toml
│   └── dist/                         ← 输出目录
├── NETLIFY_DEPLOYMENT_GUIDE.md
└── ...

Base directory 必须是：design-case-collection
```

### ❌ 问题 2：部署失败 - "npm install failed"

**原因**：package.json 有语法错误或依赖冲突

**检查方式**：
```bash
# 在本地检查
cd design-case-collection
npm install
npm run build
```

如果本地成功，应该也能在 Netlify 成功。

**解决**：
- 查看 Netlify 部署日志找到具体错误
- 修复 package.json
- 推送到 GitHub
- 重新部署

### ❌ 问题 3：每个路由都显示 404

**原因**：SPA 重定向规则未生效

**解决方案**：
1. 确保 `netlify.toml` 文件已提交到 GitHub
2. 在 Netlify 仪表盘：
   - 点击 "Deploys"
   - 找到失败的部署
   - 点击 "Redeploy"
3. 清除浏览器缓存（Ctrl+Shift+Delete）
4. 硬刷新页面（Ctrl+Shift+R）

### ❌ 问题 4：样式或图片加载失败

**原因**：资源路径错误或发布目录设置错误

**检查**：
- Publish directory 必须是：`dist`
- 在浏览器开发工具中查看失败的资源 URL
- 确保路径正确

---

## 部署成功的标志

✓ 点击 "Deploy site" 后出现绿色对勾
✓ 看到 "Site published" 消息
✓ 获得 netlify.app 域名链接
✓ 访问链接能看到应用
✓ 刷新页面不出现 404
✓ 所有功能正常工作

---

## 后续操作

### 1. 绑定自定义域名（可选）
```
仪表盘 → Site settings → Domain management → Add custom domain
```

### 2. 启用自动部署
- 已自动配置
- 每次 push 到 GitHub main/master 分支会自动部署

### 3. 查看部署历史
```
仪表盘 → Deploys → 查看所有部署记录
```

### 4. 启用分支预览（可选）
```
Site settings → Build & deploy → Preview
```
每个 pull request 都会生成预览链接

---

## 重要提醒

1. **netlify.toml** 已为你创建并配置
   - 不要删除或修改此文件
   - 已提交到 GitHub

2. **项目结构**
   - 保持 `design-case-collection` 文件夹名称不变
   - dist 会在每次构建时自动生成

3. **构建时间**
   - 首次构建可能需要 3-5 分钟
   - 后续部署通常 1-2 分钟

4. **成功几率**
   - Netlify 对 React SPA 的支持非常好
   - 如果本地能构建成功，Netlify 上也会成功
   - 99% 的概率一次成功

---

## 需要帮助？

如部署过程中遇到问题：

1. **查看 Netlify 构建日志**
   - 登录 Netlify → 点击站点 → Deploys
   - 点击失败的部署 → "Deploy log"
   - 搜索红色错误信息

2. **检查项目依赖**
   ```bash
   cd design-case-collection
   npm install
   npm run build
   ```

3. **查阅官方文档**
   - https://docs.netlify.com
   - https://docs.netlify.com/integrations/frameworks/react/

4. **社区支持**
   - Netlify Community: https://community.netlify.com
   - Stack Overflow: tag `netlify`

---

## 预期时间表

| 步骤 | 时间 | 说明 |
|------|------|------|
| 创建账户 | 2 分钟 | 如果是新用户 |
| 连接 GitHub | 1 分钟 | 授权 Netlify |
| 选择仓库 | 1 分钟 | 选择 design-case-collection |
| 配置构建 | 2 分钟 | 填写构建设置 |
| 首次部署 | 3-5 分钟 | 自动运行 |
| **总计** | **10 分钟** | 应用上线 |

---

**准备好开始了吗？登录 https://netlify.com 并按上述步骤操作！**

成功部署后，将获得类似的链接：
```
https://design-case-collection-1234.netlify.app
```

或绑定自定义域名后：
```
https://your-custom-domain.com
```

祝部署顺利！🎉
